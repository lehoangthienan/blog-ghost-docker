# BLOG EASY WITH GHOST AND DOCKER

## Configs
Change configs in docker compose file

## Install Nginx
Config domain in ngix conf

## Install Docker && Docker compose
docker-compose up -d for run

## SSL with certbot
